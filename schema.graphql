type Account @entity {
  "Account address"
  id: ID!
  balance: BigInt!
  historicalBalances: [HistoricalBalance!] @derivedFrom(field: "account")
}

type HistoricalBalance @entity {
  id: ID!
  account: Account!
  balance: BigInt!
  date: DateTime!
}

type Neuron @entity {
  "Neuron address"
  id: ID!
  version: String!
  ip: String!
  port: Number!
  ip_type: BigInt!
  uid: BigInt!
  modality: BigInt!
  hotkey: String!
  coldkey: String!
  active: BigInt!
  last_update: BigInt!
  priority: BigInt!
  stake: Float!
  rank: Float!
  trust: Float!
  consensus: Float!
  incentive: Float!
  dividends: Float!
  emission: Float!
  bonds: [Float]!
  weights: [Float]!
}


